<Window x:Class="AddinVeMong.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông tin phần mềm" Height="400" Width="550" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
        Background="{DynamicResource WindowBackgroundColor}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AddinVeMong;component/Resources/Styles/LightTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ProjectTitle}" 
                   FontSize="22" FontWeight="Bold" 
                   Foreground="{DynamicResource TextPrimaryColor}" 
                   HorizontalAlignment="Center" Margin="0,0,0,15" Grid.Row="0"/>

        <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="{DynamicResource BorderColor}"/>

        <StackPanel Grid.Row="1" Margin="0,15,0,15">
            <TextBlock Text="Giới thiệu &amp; Chức năng:" FontWeight="Bold" FontSize="14" 
                       Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Description}" 
                       FontSize="13" TextWrapping="Wrap" LineHeight="20" 
                       Foreground="{DynamicResource TextPrimaryColor}"/>
        </StackPanel>

        <Border Grid.Row="2" Background="{DynamicResource ControlBackgroundColor}" 
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="5" Padding="15">
            <StackPanel>
                <TextBlock Text="Thành viên thực hiện:" FontWeight="Bold" FontSize="14" 
                           Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding MemberPlaceholder}" 
                           FontSize="13" LineHeight="22"
                           Foreground="{DynamicResource TextPrimaryColor}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding VersionInfo}" VerticalAlignment="Center" 
                       Foreground="{DynamicResource TextSecondaryColor}" FontStyle="Italic"/>

            <Button Content="Đóng" Width="100" Height="32" 
                    HorizontalAlignment="Right"
                    Background="{DynamicResource PrimaryButtonBgColor}" 
                    Foreground="{DynamicResource PrimaryButtonFgColor}"
                    FontWeight="Bold" IsDefault="True" IsCancel="True">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>