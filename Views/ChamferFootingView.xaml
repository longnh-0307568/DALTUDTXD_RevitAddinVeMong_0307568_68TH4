<Window x:Class="AddinVeMong.Views.ChamferFootingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông số móng đơn vát" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="THIẾT LẬP KÍCH THƯỚC MÓNG" 
                   FontSize="16" FontWeight="Bold" Foreground="#1565C0" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Chiều dài đáy L (mm):" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Length}" Margin="5" VerticalContentAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Chiều rộng đáy B (mm):" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Width}" Margin="5" VerticalContentAlignment="Center"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Chiều dài đỉnh l (mm):" VerticalAlignment="Center" Margin="0,10,0,0" FontWeight="Bold"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Dài đỉnh l (mm):" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding TopLength}" Margin="5" VerticalContentAlignment="Center"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Rộng đỉnh b (mm):" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TopWidth}" Margin="5" VerticalContentAlignment="Center"/>

            <Label Grid.Row="5" Grid.Column="0" Content="Chiều cao đế/vát (h1, h2, h3):" VerticalAlignment="Center" Margin="0,10,0,0" FontWeight="Bold"/>

            <StackPanel Grid.Row="6" Grid.ColumnSpan="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="2">
                        <TextBlock Text="h1 (đế)" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding HBase}" Margin="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="2">
                        <TextBlock Text="h2 (thẳng)" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding HStraight}" Margin="2"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="2">
                        <TextBlock Text="h3 (vát)" HorizontalAlignment="Center"/>
                        <TextBox Text="{Binding HChamfer}" Margin="2"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Hủy" Width="80" Height="30" Margin="10,0" IsCancel="True"/>
            <Button Content="Vẽ móng" 
                    Command="{Binding CreateCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="100" Height="30" Background="#1565C0" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>