<Window x:Class="AddinVeMong.Views.ConcentricRebarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Thiết lập cốt thép móng đúng tâm" Height="780" Width="650" 
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AddinVeMong;component/Resources/Styles/TextBoxStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CẤU HÌNH THÉP MÓNG ĐÚNG TÂM" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Foreground="#1565C0" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <Expander Header="1. THÔNG SỐ CHUNG (MÓNG &amp; BÊ TÔNG)" IsExpanded="True" Margin="0,5" FontWeight="Bold">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <Label Content="Lớp bê tông bảo vệ (mm):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding Cover}" Grid.Row="0" Grid.Column="1" Margin="5,2"
                                 Tag="Mặc định: 50"
                                 Style="{StaticResource PlaceholderTextBoxStyle}" VerticalContentAlignment="Center"/>
                    </Grid>
                </Expander>

                <Expander Header="2. THÉP CẠNH DÀI (PHƯƠNG X)" IsExpanded="True" Margin="0,5" FontWeight="Bold">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <Label Content="Đường kính (mm):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <ComboBox SelectedValue="{Binding LongDiameter}" Grid.Row="0" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center">
                            <sys:Int32>10</sys:Int32>
                            <sys:Int32>12</sys:Int32>
                            <sys:Int32>14</sys:Int32>
                            <sys:Int32>16</sys:Int32>
                            <sys:Int32>18</sys:Int32>
                            <sys:Int32>20</sys:Int32>
                        </ComboBox>

                        <Label Content="Khoảng cách a (mm):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding LongSpacing}" Grid.Row="1" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>

                        <Label Content="Chiều dài bẻ móc (mm):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding LongHookLength}" Grid.Row="2" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>
                    </Grid>
                </Expander>

                <Expander Header="3. THÉP CẠNH NGẮN (PHƯƠNG Y)" IsExpanded="True" Margin="0,5" FontWeight="Bold">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <Label Content="Đường kính (mm):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <ComboBox SelectedValue="{Binding ShortDiameter}" Grid.Row="0" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center">
                            <sys:Int32>10</sys:Int32>
                            <sys:Int32>12</sys:Int32>
                            <sys:Int32>14</sys:Int32>
                            <sys:Int32>16</sys:Int32>
                            <sys:Int32>18</sys:Int32>
                        </ComboBox>

                        <Label Content="Khoảng cách a (mm):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding ShortSpacing}" Grid.Row="1" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>

                        <Label Content="Chiều dài bẻ móc (mm):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding ShortHookLength}" Grid.Row="2" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>
                    </Grid>
                </Expander>

                <Expander Header="4. THÉP CỔ CỘT &amp; ĐAI (STARTER BARS)" IsExpanded="True" Margin="0,5" FontWeight="Bold">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <Label Content="Kích thước cột X (mm):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding ColumnWidthX}" Grid.Row="0" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"
                                 Tag="(Ví dụ: 220, 300)"
                                 Style="{StaticResource PlaceholderTextBoxStyle}"/>

                        <Label Content="Kích thước cột Y (mm):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding ColumnWidthY}" Grid.Row="1" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"
                                 Tag="(Ví dụ: 220, 400)"
                                 Style="{StaticResource PlaceholderTextBoxStyle}"/>

                        <Label Content="Đường kính thép dọc:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <ComboBox SelectedValue="{Binding StarterDiameter}" Grid.Row="2" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center">
                            <sys:Int32>16</sys:Int32>
                            <sys:Int32>18</sys:Int32>
                            <sys:Int32>20</sys:Int32>
                            <sys:Int32>22</sys:Int32>
                            <sys:Int32>25</sys:Int32>
                        </ComboBox>

                        <Label Content="Chiều dài chân vịt (mm):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding StarterHookLength}" Grid.Row="3" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>

                        <Label Content="Chiều dài chờ cột (mm):" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding StarterLength}" Grid.Row="4" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>

                        <Label Content="Đường kính thép đai:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <ComboBox SelectedValue="{Binding StirrupDiameter}" Grid.Row="5" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center">
                            <sys:Int32>6</sys:Int32>
                            <sys:Int32>8</sys:Int32>
                            <sys:Int32>10</sys:Int32>
                        </ComboBox>

                        <Label Content="Khoảng cách đai (mm):" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" FontWeight="Normal"/>
                        <TextBox Text="{Binding StirrupSpacing}" Grid.Row="6" Grid.Column="1" Margin="5,2" VerticalContentAlignment="Center"/>
                    </Grid>
                </Expander>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Hủy" Width="80" Height="30" Margin="10,0" IsCancel="True"/>
            <Button Content="Tạo Thép" 
                    Command="{Binding DrawRebarCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="100" Height="30" Background="#1565C0" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>